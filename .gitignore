# Ignorar directorios node_modules
backend/node_modules/
frontend/chispasTienda/node_modules/

# Ignorar archivos de entorno locales
.env
.env.local

# Ignorar archivos de build/dist
# Para el frontend (Vite genera 'dist')
frontend/chispasTienda/dist/

# Para el backend si generas algún build o logs
backend/dist/
backend/logs/

# Ignorar archivos de logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Ignorar archivos de cache de IDEs (ej. VS Code)
.vscode/
.DS_Store # Para usuarios de macOS

# Ignorar archivos de lock generados automáticamente (si quieres forzar solo package.json)
# package-lock.json # Descomenta si usas yarn.lock y no quieres package-lock
# yarn.lock